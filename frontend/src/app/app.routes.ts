import { Routes } from '@angular/router';
import { ProductComponent } from './product/product.component';

export const routes: Routes = [  
    // redirect to `first-component`
    {path: "Product2", component: ProductComponent}
];
